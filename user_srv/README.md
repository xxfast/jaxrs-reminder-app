# User Service

## Requirements

 * Python 3.10
 * Docker

`$ pip install -r requirements.txt`

## Run on development environment

`$ flask run`

## Run unit tests

`$ pytest`

## Build docker images

`$ docker-compose build`

## Run docker images

`$ docker-compose up`
